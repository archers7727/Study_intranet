# 태그 기반 교육 관리 시스템 배포 계획서

## 📋 목차
1. [서비스 개요](#서비스-개요)
2. [기술 스택 선정](#기술-스택-선정)
3. [데이터베이스 설계](#데이터베이스-설계)
4. [배포 아키텍처](#배포-아키텍처)
5. [비용 분석](#비용-분석)
6. [확장 전략](#확장-전략)
7. [개발 로드맵](#개발-로드맵)

---

## 서비스 개요

**예상 사용자:** 월 100명 (교사 10명, 학생 70명, 학부모 20명)
**동시 접속자:** 평균 3-5명, 최대 15-20명 (수업 시간대)
**주요 기능:** 학생/클래스/세션 관리, 출결, 과제, 태그 기반 검색, RBAC 권한 시스템

---

## 기술 스택 선정

### 🎨 프론트엔드
**선택: Next.js 14 (App Router) + TypeScript**

**추천 UI 라이브러리:**
- **shadcn/ui** + Tailwind CSS (무료, 커스터마이징 용이)
- **Radix UI** (접근성 뛰어남)
- **React Hook Form** + Zod (폼 관리 및 검증)
- **TanStack Query** (서버 상태 관리)
- **FullCalendar** 또는 **React Big Calendar** (캘린더 뷰)

**이유:**
- SSR/SSG로 초기 로딩 속도 최적화
- API Routes로 간단한 백엔드 로직 처리 가능
- Vercel 무료 배포 지원

---

### ⚙️ 백엔드
**선택: Next.js API Routes + Prisma ORM**

**대안 (필요시 분리):**
- **Hono** + Cloudflare Workers (무료 100,000 req/day)
- **tRPC** (타입 안전한 API)

**이유:**
- Next.js API Routes로 백엔드 통합 (별도 서버 불필요)
- Prisma로 타입 안전한 DB 쿼리
- Serverless 구조로 자동 스케일링

---

### 🗄️ 데이터베이스
**선택: Supabase (PostgreSQL)**

**무료 티어 제공:**
- ✅ 500MB 데이터베이스
- ✅ 5GB 파일 스토리지
- ✅ 50,000 MAU (Monthly Active Users)
- ✅ Row Level Security (RLS) 지원
- ✅ Realtime 기능 (선택적 사용)
- ✅ 내장 인증 시스템

**예상 데이터 사용량:**
```
학생 100명 × 2KB = 200KB
교사 10명 × 2KB = 20KB
클래스 20개 × 5KB = 100KB
세션 (3개월) 200개 × 3KB = 600KB
출석 데이터 (6개월) 10,000건 × 0.5KB = 5MB
과제 데이터 5,000건 × 1KB = 5MB
태그/자료 등 = 2MB

총 예상: 약 13MB (500MB의 2.6%)
```

**대안 (향후 확장 시):**
- **Neon** (PostgreSQL, 무료 3GB, Serverless)
- **PlanetScale** (MySQL, 무료 5GB)

---

### 📦 파일 스토리지
**선택: Supabase Storage (5GB 무료)**

**용도:**
- 수업 자료 (PDF, PPT 등)
- 학생 프로필 사진
- 과제 제출 파일

**대안:**
- **Cloudflare R2** (무료 10GB, egress 무료)

---

### 🔐 인증 시스템
**선택: Supabase Auth**

**기능:**
- 이메일/비밀번호 인증
- 세션 관리
- Row Level Security 연동
- 커스텀 사용자 메타데이터 (권한 레벨 저장)

**대안:**
- **NextAuth.js v5** (Auth.js)

---

## 데이터베이스 설계

### ERD 주요 테이블

```sql
-- 사용자 (Supabase Auth 연동)
users (id, email, name, role_level, created_at)

-- 학생
students (
  id, user_id, student_id(custom),
  name, birth_date, gender, grade, school,
  enrollment_status, management_status,
  phone, parent_id, tags[]
)

-- 보호자
parents (
  id, user_id, name, phone, relation,
  student_ids[], notification_settings
)

-- 교사
teachers (
  id, user_id, name, phone,
  role_level, specialties[]
)

-- 클래스
classes (
  id, name, main_teacher_id, assistant_teacher_ids[],
  cost, description, tags[],
  recurring_schedules (JSONB), is_active
)

-- 세션 (개별 수업)
sessions (
  id, class_id, session_date, start_time, end_time,
  location, status, tags[], notes
)

-- 출석
attendances (
  id, session_id, student_id,
  status (present/absent), checked_at, checked_by
)

-- 과제
assignments (
  id, session_id, title, description,
  due_date, tags[], created_by
)

-- 과제 제출
submissions (
  id, assignment_id, student_id,
  status, submitted_at, score, graded_at, graded_by,
  file_url
)

-- 자료
materials (
  id, title, description, file_url,
  tags[], created_by, created_at
)

-- 세션-자료 연결
session_materials (session_id, material_id)

-- 태그
tags (
  id, name, color, category,
  usage_count, created_at
)

-- 상태 변경 이력
status_logs (
  id, student_id, previous_status, new_status,
  reason, changed_by, changed_at
)
```

### 인덱스 전략

```sql
-- 자주 조회되는 필드에 인덱스
CREATE INDEX idx_students_enrollment ON students(enrollment_status);
CREATE INDEX idx_sessions_date ON sessions(session_date);
CREATE INDEX idx_attendances_session ON attendances(session_id);
CREATE INDEX idx_assignments_due ON assignments(due_date);

-- 태그 검색 최적화 (GIN 인덱스)
CREATE INDEX idx_students_tags ON students USING GIN(tags);
CREATE INDEX idx_classes_tags ON classes USING GIN(tags);
CREATE INDEX idx_sessions_tags ON sessions USING GIN(tags);
```

---

## 배포 아키텍처

### 🏗️ 추천 아키텍처 (풀스택 Next.js)

```
┌─────────────────────────────────────────────┐
│         사용자 (교사/학생/학부모)              │
└──────────────────┬──────────────────────────┘
                   │
         ┌─────────▼──────────┐
         │   Vercel CDN       │ (무료)
         │   (전세계 배포)     │
         └─────────┬──────────┘
                   │
    ┌──────────────▼───────────────┐
    │   Next.js 14 Application     │
    │  ┌────────────────────────┐  │
    │  │  Frontend (SSR/CSR)    │  │ Vercel
    │  ├────────────────────────┤  │ (무료)
    │  │  API Routes (백엔드)   │  │
    │  │  + Prisma ORM          │  │
    │  └────────────────────────┘  │
    └──────────────┬───────────────┘
                   │
         ┌─────────▼──────────┐
         │  Supabase          │ (무료)
         │  ┌──────────────┐  │
         │  │ PostgreSQL   │  │
         │  ├──────────────┤  │
         │  │ Auth         │  │
         │  ├──────────────┤  │
         │  │ Storage      │  │
         │  └──────────────┘  │
         └────────────────────┘
```

### 🔄 대안 아키텍처 (백엔드 분리 시)

프론트엔드와 백엔드를 분리해야 할 경우:

**프론트엔드:** Vercel (Next.js)
**백엔드:** Railway 무료 티어 또는 Fly.io
**데이터베이스:** Supabase

---

## 비용 분석

### 💰 월별 예상 비용 (100명 사용자 기준)

| 서비스 | 무료 티어 | 예상 사용량 | 비용 |
|--------|-----------|-------------|------|
| **Vercel** | 100GB 대역폭, 100GB-hours | 10GB 대역폭 | **$0** |
| **Supabase** | 500MB DB, 5GB Storage | 13MB DB, 1GB Storage | **$0** |
| **도메인** | - | .com 도메인 | **$12/년 (~$1/월)** |

**총 월 비용: $0~1** (도메인 제외 시 완전 무료)

### 📈 사용자 증가 시 비용 예측

**300명 사용자 (무료 범위 초과 시):**
- Vercel: 무료 유지 가능 (대역폭 100GB 내)
- Supabase: $25/월 (Pro Plan - 8GB DB, 100GB Storage)
- **총: $25/월**

**1,000명 사용자:**
- Vercel: 무료 유지 가능
- Supabase: $25/월 (충분)
- CDN 추가 (Cloudflare): 무료
- **총: $25/월**

---

## 확장 전략

### 🚀 단계별 확장 계획

#### Phase 1: MVP (0-100명)
- Next.js + Supabase 올인원
- 완전 무료 운영
- 모니터링: Vercel Analytics (무료)

#### Phase 2: 성장기 (100-500명)
- Supabase Pro 업그레이드 ($25/월)
- 커스텀 도메인 + SSL
- 백업 자동화 (Supabase 내장)

#### Phase 3: 확장기 (500-2,000명)
- Database Connection Pooling (Supabase Pooler)
- CDN 캐싱 최적화
- 이미지 최적화 (Next.js Image)

#### Phase 4: 대규모 (2,000명+)
- Read Replica 추가 (Supabase)
- Redis 캐시 추가 (Upstash 무료 10,000 req/day)
- 백엔드 분리 고려

### 🔧 성능 최적화 전략

1. **데이터베이스 최적화**
   - 페이지네이션 (무한 스크롤)
   - Lazy Loading
   - 쿼리 최적화 (N+1 문제 해결)

2. **프론트엔드 최적화**
   - ISR (Incremental Static Regeneration)
   - 이미지 최적화 (WebP, lazy load)
   - 코드 스플리팅
   - TanStack Query 캐싱

3. **파일 스토리지 최적화**
   - 파일 크기 제한 (10MB/파일)
   - 압축 업로드
   - CDN 캐싱

---

## 개발 로드맵

### 🗓️ 1단계: 기반 구축 (1-2주)

**백엔드 설정**
- [ ] Next.js 프로젝트 초기화
- [ ] Supabase 프로젝트 생성 및 연동
- [ ] Prisma 스키마 설계 및 마이그레이션
- [ ] Supabase Auth 설정
- [ ] Row Level Security (RLS) 정책 설정

**프론트엔드 설정**
- [ ] UI 라이브러리 설치 (shadcn/ui)
- [ ] 레이아웃 구조 설계
- [ ] 라우팅 구조 설계
- [ ] 인증 플로우 구현

---

### 🗓️ 2단계: 핵심 기능 개발 (3-4주)

**권한 시스템 (RBAC)**
- [ ] 6단계 권한 레벨 구현
- [ ] 미들웨어 기반 권한 체크
- [ ] 역할별 UI 표시 제어

**학생 관리**
- [ ] 학생 CRUD
- [ ] 자동 ID/비밀번호 생성
- [ ] 학년 자동 계산
- [ ] 보호자 연동
- [ ] 태그 할당

**클래스 관리**
- [ ] 클래스 CRUD
- [ ] 교사 배정
- [ ] 반복 일정 설정
- [ ] 클래스 복사 기능

**태그 시스템**
- [ ] 태그 CRUD
- [ ] 컬러 피커
- [ ] 사용량 카운트
- [ ] AND/OR 검색 로직

---

### 🗓️ 3단계: 세션 및 출결 관리 (2-3주)

**세션 관리**
- [ ] 세션 자동 생성 도구
- [ ] 캘린더 뷰 (월/주/일)
- [ ] 모바일 아젠다 뷰
- [ ] 세션 수정/취소

**출석 관리**
- [ ] 출결 체크 UI
- [ ] 일괄 출석 처리
- [ ] 출석률 계산
- [ ] 출석 이력 조회

---

### 🗓️ 4단계: 과제 및 자료 관리 (2-3주)

**과제 시스템**
- [ ] 과제 부여 (세션별)
- [ ] 태그 기반 과제 추천
- [ ] 과제 제출 (학생)
- [ ] 수동 채점 (교사)
- [ ] 학생별/과제별 뷰 전환
- [ ] 마감일 관리 및 알림

**자료 관리**
- [ ] 자료 업로드 (Supabase Storage)
- [ ] 세션-자료 연결
- [ ] 태그 기반 자료 추천
- [ ] 파일 다운로드

---

### 🗓️ 5단계: 부가 기능 (1-2주)

**대시보드**
- [ ] 교사용 대시보드 (출석률, 과제 현황)
- [ ] 학생/학부모용 대시보드
- [ ] 통계 차트 (Chart.js/Recharts)

**모바일 최적화**
- [ ] 반응형 디자인 검증
- [ ] 터치 최적화
- [ ] PWA 설정 (선택)

**알림 시스템 (선택)**
- [ ] 이메일 알림 (Resend 무료 100통/일)
- [ ] 과제 마감 알림
- [ ] 출결 알림 (학부모)

---

### 🗓️ 6단계: 테스트 및 배포 (1주)

**테스트**
- [ ] E2E 테스트 (Playwright/Cypress)
- [ ] 단위 테스트 (Vitest)
- [ ] 권한 테스트

**배포**
- [ ] Vercel 배포
- [ ] 환경 변수 설정
- [ ] 도메인 연결
- [ ] SSL 인증서

**모니터링**
- [ ] Vercel Analytics
- [ ] Sentry (에러 추적, 무료 5,000 이벤트/월)
- [ ] Supabase Dashboard 모니터링

---

## 추가 권장사항

### 🛡️ 보안 고려사항
- [ ] HTTPS 강제
- [ ] CSRF 보호 (Next.js 내장)
- [ ] XSS 방지 (React 자동 이스케이핑)
- [ ] SQL Injection 방지 (Prisma ORM)
- [ ] Rate Limiting (Vercel Edge Config)
- [ ] 파일 업로드 검증 (파일 타입, 크기)

### 📱 접근성
- [ ] ARIA 레이블
- [ ] 키보드 네비게이션
- [ ] 스크린 리더 지원

### 📊 분석 도구
- Vercel Analytics (무료)
- Google Analytics 4 (선택)
- Posthog (오픈소스, self-hosted 가능)

---

## 결론

이 아키텍처는 **완전 무료 운영이 가능**하며, 사용자가 증가해도 **월 $25 이하로 유지** 가능합니다.

**핵심 장점:**
✅ 초기 비용 $0 (도메인 제외)
✅ 자동 스케일링 (Serverless)
✅ 글로벌 CDN (빠른 로딩)
✅ 타입 안전성 (TypeScript + Prisma)
✅ 개발 생산성 (Next.js + shadcn/ui)
✅ 보안성 (Supabase RLS)

**예상 개발 기간:** 8-12주 (1명 풀타임 기준)
